# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# Environment / secrets
.env
*.env

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs / data dumps
*.log
*.csv

# SQLite time-series store (snapshots / IV / strategy context)
*.db
*.db-wal
*.db-shm

# Paper trading state (local virtual portfolio)
paper_state.json

# Recommendation simulator state (forward-test ledger)
sim_state.json

# Live-feed broker credentials — NEVER commit
# Angel One SmartAPI (api key + client code + mpin + totp secret)
angel_config.json
# Dhan (client id + access token)
dhan_config.json

# Angel One SmartAPI SDK writes per-day tick logs here
logs/
